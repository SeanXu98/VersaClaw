# VersaClaw Docker Compose
# 一键部署前后端服务

services:
  # ============================================
  # Backend Service - Nanobot API Server
  # ============================================
  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    image: versaclaw-backend:latest
    container_name: versaclaw-backend
    restart: unless-stopped
    ports:
      - "18790:18790"
    volumes:
      # Persist Nanobot configuration and data
      - versaclaw-data:/root/.nanobot
      # Or mount from host (choose one):
      # - ~/.nanobot:/root/.nanobot
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONUTF8=1
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:18790/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    networks:
      - versaclaw-network

  # ============================================
  # Frontend Service - Next.js Web UI
  # ============================================
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    image: versaclaw-frontend:latest
    container_name: versaclaw-frontend
    restart: unless-stopped
    ports:
      - "5000:5000"
    environment:
      - NODE_ENV=production
      - NEXT_PUBLIC_APP_NAME=VersaClaw
      - NEXT_PUBLIC_APP_DESCRIPTION=多模态 AI Agent 可视化管理平台
      # Backend API URL (use service name for Docker networking)
      - NANOBOT_API_URL=http://backend:18790
    depends_on:
      backend:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:5000"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    networks:
      - versaclaw-network

# ============================================
# Networks
# ============================================
networks:
  versaclaw-network:
    driver: bridge

# ============================================
# Volumes
# ============================================
volumes:
  versaclaw-data:
    driver: local
